services:
  - type: web
    name: telegram-links-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: RENDER
        value: true
    healthCheckPath: /health
    autoDeploy: true
